module.exports = {
  //local MongoDB deployment ->
  URI:
    "mongodb://127.0.0.1/book_store"
  // "URI": "mongodb://localhost/books229"
  // update the URI to the latest db
  
};
// db name : book_store
// collection name : books
// Title(S), Description(S), Price(N), Author(S), Genre(S)